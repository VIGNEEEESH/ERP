// Define the possible statuses and priorities
const statuses = ['todo', 'in-progress', 'done'];
const priorities = ['low', 'medium', 'high'];

// Export the statuses and priorities
export { statuses, priorities };

// Define a Task object as a JavaScript object
const Task = {
  title: '',
  desc: '',
  id: '',
  priority: '',
  status: '',
};

// Export the Task object
export { Task };
